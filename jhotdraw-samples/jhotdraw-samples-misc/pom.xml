<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.jhotdraw</groupId>
        <artifactId>jhotdraw-samples</artifactId>
        <version>9.1-SNAPSHOT</version>
    </parent>
    <artifactId>jhotdraw-samples-misc</artifactId>
    <packaging>jar</packaging>
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>jhotdraw-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.devzendo</groupId>
            <artifactId>Quaqua</artifactId>
            <version>7.3.4</version>
        </dependency>
        <dependency>
            <groupId>net.sourceforge.htmlunit</groupId>
            <artifactId>htmlunit</artifactId>
            <version>2.37.0</version>
        </dependency>
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>1.9.9</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>jhotdraw-app</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
<plugin>
     <groupId>org.codehaus.mojo</groupId>
     <artifactId>exec-maven-plugin</artifactId>
     <version>3.1.0</version>
     <configuration>
          <!--
            htmlunit pulls in the old standalone Xerces (xerces:xercesImpl /
            xml-apis), whose META-INF/services registers a DocumentBuilderFactory
            that is "not a subtype" of JDK 21's java.xml DocumentBuilderFactory.
            Force the JDK's built-in parser so the app's XML/preferences code
            (AbstractApplication.init) does not pick up the broken provider.
          -->
          <systemProperties>
               <systemProperty>
                    <key>javax.xml.parsers.DocumentBuilderFactory</key>
                    <value>com.sun.org.apache.xerces.internal.jaxp.DocumentBuilderFactoryImpl</value>
               </systemProperty>
               <systemProperty>
                    <key>javax.xml.parsers.SAXParserFactory</key>
                    <value>com.sun.org.apache.xerces.internal.jaxp.SAXParserFactoryImpl</value>
               </systemProperty>
          </systemProperties>
     </configuration>
</plugin>
        </plugins>
    </build>
</project>